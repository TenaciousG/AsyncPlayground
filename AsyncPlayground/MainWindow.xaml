<Window x:Class="AsyncPlayground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncPlayground"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <StackPanel DataContext="{StaticResource MainViewModel}" Margin="0,20,0,0">
        <Button Grid.Row="0" Command="{Binding FetchSomethingCommand}" Content="FetchSomething" Width="100"/>
        <TextBlock Text="{Binding FetchResult}"/>
        </StackPanel>
    </Window>
